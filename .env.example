# Domaine public pointant vers ce serveur (A/AAAA)
DOMAIN=example.com

# Nginx server_name (peut inclure plusieurs noms séparés par des espaces)
NGINX_SERVER_NAME="example.com www.example.com"

# Nom du certificat Let's Encrypt (répertoire sous /etc/letsencrypt/live/)
LE_CERT_NAME=example.com

# Email Let's Encrypt
LE_EMAIL=admin@example.com

# Optionnel: plusieurs domaines (SAN), séparés par des espaces
# LE_DOMAINS="example.com www.example.com"

# 1 = environnement de test Let's Encrypt (évite les limites de taux)
LE_STAGING=0

# (Recommandé) Mets des secrets non-triviaux en prod
MARIADB_ROOT_PASSWORD=change-me
WORDPRESS_DB_PASSWORD=change-me

# Version de PHP-FPM pour le conteneur app.
# Pour coller à un ancien serveur, essaie d'abord 7.4 (souvent plus simple que 5.6).
# Exemples: PHP_VERSION=8.2 | PHP_VERSION=7.4
PHP_VERSION=8.2
